#DEV environment

app:
    message: This is the property file for the ${spring.application.name} specific to DEV

spring:
    datasource:
        driver-class-name: org.h2.Driver
        url=: jdbc:h2
        username: sa
        password: sa
